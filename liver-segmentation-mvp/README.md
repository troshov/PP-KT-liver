# Liver Segmentation MVP

Веб-сервис для автоматической сегментации печени на КТ-снимках.

## Быстрый старт

### Требования
- Docker и Docker Compose
- Python 3.10+ (для локальной разработки)
- Node.js 18+ (для фронтенда)

### Установка и запуск

**С Docker Compose:**
```bash
docker-compose up --build
```

Сервис будет доступен по адресу:
- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:8000
- **API Docs:** http://localhost:8000/docs

**Локально (без Docker):**

Backend:
```bash
cd backend
pip install -r requirements.txt
python main.py
```

Frontend:
```bash
cd frontend
npm install
npm run dev
```

---

## Структура проекта

```
liver-segmentation-mvp/
├── backend/
│   ├── main.py              # FastAPI приложение
│   ├── models.py            # U-Net архитектура
│   ├── inference.py         # Обработка и инференс
│   ├── requirements.txt      # Python зависимости
│   ├── Dockerfile
│   ├── weights/             # Папка для весов моделей
│   ├── uploads/             # Временные загрузки
│   └── results/             # Результаты сегментации
│
├── frontend/
│   ├── src/
│   │   ├── main.tsx         # React entry point
│   │   ├── App.tsx          # Главный компонент
│   │   ├── index.css        # Стили
│   │   ├── App.css          # Дополнительные стили
│   │   └── components/
│   │       ├── FileUploader.tsx    # Загрузчик файлов
│   │       ├── ImageViewer.tsx     # Просмотр результатов
│   │       └── MetricsPanel.tsx    # Метрики сегментации
│   ├── index.html
│   ├── package.json
│   ├── vite.config.ts
│   ├── Dockerfile
│   └── tsconfig.json
│
├── docker-compose.yml
└── README.md

```

---

## Использование API

### Загрузка файла и сегментация

```bash
curl -X POST http://localhost:8000/upload \
  -F "file=@your_dicom_file.dcm"
```

**Ответ:**
```json
{
  "result_id": "a1b2c3d4",
  "status": "success",
  "mask_url": "/results/a1b2c3d4/mask.png",
  "original_url": "/results/a1b2c3d4/original.png",
  "metrics": {
    "area_pixels": 45230,
    "volume_mm3": 90460.0,
    "volume_ml": 90.46
  }
}
```

### Получить маску

```bash
curl http://localhost:8000/results/{result_id}/mask.png -o mask.png
```

### Здоровье API

```bash
curl http://localhost:8000/health
```

---

## Технологии

| Компонент | Технология |
| :--- | :--- |
| **Backend** | FastAPI, PyTorch, SimpleITK |
| **Frontend** | React, TypeScript, Vite |
| **Визуализация** | PIL, NumPy |
| **Оркестрация** | Docker, Docker Compose |

---

## ML-часть

### Модель: U-Net
- **Архитектура:** Encoder-Decoder с skip connections
- **Входной размер:** 256×256
- **Выходной размер:** Маска сегментации (256×256)
- **Обучение:** На датасете CHAOS

### Препроцессинг
1. Конвертация DICOM в numpy
2. Нормализация (печеночное окно: 40±200 HU)
3. Ресайз до 256×256

### Постпроцессинг
1. Применение threshold (0.5)
2. Ресайз обратно к исходному размеру
3. Расчет метрик

---

## Результаты

- **Dice Coefficient (валидация):** ~0.82
- **Скорость инференса (GPU):** ~300 мс
- **Скорость инференса (CPU):** ~1-2 сек

---

## Конфигурация

**Переменные окружения** (опционально):
```bash
CUDA_VISIBLE_DEVICES=0  # GPU ID (если есть)
MODEL_PATH=weights/unet_best.pt
```

---

## Отладка

### Логи
```bash
docker-compose logs -f
```

### Тестирование API
Документация доступна по адресу: http://localhost:8000/docs

---

## Лицензия

Проект разработан в рамках учебного курса УрФУ.

---

## 👥 Команда

- **ML-специалист:** Илья
- **Fullstack разработчик:** Сергей
- **Тимлид:** Дима